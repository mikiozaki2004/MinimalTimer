# 2026-04-08

### やったこと

#### Excel / Google Sheets 連携
- 記録ボタン右クリックメニューの連携設定を「Google Sheets 設定」から「Excel / Sheets 連携」に変更
- GAS Web App URL だけでなく、Power Automate の HTTP POST URL も貼れる共通URL連携として整理
- タイマー完了時と過去ログ送信時の送信形式を配列JSONに統一
  - Power Automate 側で `Apply to each` しやすい形にした
  - 既存の Google Sheets GAS は配列にも対応しているため互換性を維持
- `docs/google-apps-script.js` の案内文を新メニュー名に更新

#### Excel 連携ドキュメントとテンプレート
- `docs/excel-power-automate.md` を追加
  - OneDrive / SharePoint 上の Excel に Power Automate で追記する手順を記録
  - Google Drive for desktop の同期フォルダ内 Excel ファイルへ直接追記する手順も追記
- `docs/MinimalTimer_WorkLogs.xlsx` を追加
  - シート名: `作業記録`
  - テーブル名: `WorkLogs`
  - 列: `日付`, `タスク`, `詳細`, `開始`, `終了`, `時間(分)`, `休憩`

#### Google Drive 上の Excel へ直接追記
- `src/index.html` / `src/style.css`
  - 連携パネルに「ローカルExcel」パス入力欄を追加
  - `Excelへ過去の記録を追記` ボタンを追加
- `src/store.js`
  - `mt_excel_path` を永続化対象に追加
- `src/main.js`
  - Excelパスを保存し、タイマー完了時にローカルExcelへ自動追記する処理を追加
  - 過去ログを一括でExcelへ追記する処理を追加
  - パス貼り付け時の前後の引用符を削除するようにした
  - 古いログ形式向けに `isBreak` 欠落時は `false`、`startedAt` 欠落時は `timestamp` / `duration` から補完するようにした
- `src-tauri/src/lib.rs`
  - `append_excel_records` Tauriコマンドを追加
  - Windows の Excel COM 経由で `WorkLogs` テーブルへ行を追加
  - `WorkLogs` テーブルが無い場合は作成
  - PowerShellスクリプトの文字化け対策として、日本語ラベルを引数で渡す形に変更
  - `isBreak` が無い古いログも `false` として受け取れるようにした

#### 検証
- `node --input-type=module --check` で `src/main.js` の構文確認
- `cargo check` 成功
- `npm run build` 成功
- Excelテンプレートのコピーに対して、Excel COM 経由で `WorkLogs` テーブルへ1行追記できることを確認

### 次回やること

- 実運用の Google Drive 同期フォルダ内 Excel で追記を再確認
- Excelファイルを開いている場合の失敗表示をもう少し分かりやすくする
- 可能ならファイルパス入力を手入力ではなくファイル選択ダイアログにする

---

## セッション2

### やったこと

#### Excel 送信時のUX改善
- PowerShell プロセス起動時に `CREATE_NO_WINDOW` フラグ (`0x08000000`) を設定し、送信中にターミナルが一瞬表示される問題を修正
- `append_excel_records` Tauriコマンドを `async` 化し、`spawn_blocking` でバックグラウンドスレッドに移動
  - 送信中にアプリが操作不能になる問題を修正
- `std::os::windows::process::CommandExt` トレイトを Windows 限定でインポート

### 次回やること

なし
