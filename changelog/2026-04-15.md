# 2026-04-15

### やったこと
- ポモドーロ設定を別ウィンドウに分離
  - `pomo.html` / `pomo.css` / `pomo.js` を新規作成（240×160px のウィンドウ）
  - `tauri.conf.json` に `pomo` ウィンドウを追加
  - `lib.rs` に `open_pomo_window` コマンドを追加・登録、close イベントを hide に変換
  - `index.html` のインライン `pomo-panel` を削除
  - `main.js` のパネル処理を削除し、`open_pomo_window` 呼び出しと `pomo-start` イベントリスナーに置き換え
  - `style.css` の不要な pomo パネル CSS を削除
  - タスク選択ウィンドウと同様に、ポモドーロボタンでメインウィンドウ隣に新規ウィンドウが開く動作を実現

### 次回やること
