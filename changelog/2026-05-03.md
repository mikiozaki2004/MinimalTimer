# 2026-05-03

### やったこと

#### タイムライン（1日の時間の使い方を可視化する機能）を追加
- 記録ウィンドウの日別詳細を `task-bars`（上位3タスクの集計バー）からタイムラインバーに置き換え
- 横軸 0:00–24:00、既存タイマーログをカテゴリ色で自動セグメント化
- 5分以上のギャップを点線枠の `?` セグメントとしてクリック可能に
- カテゴリ8種を定義（睡眠／食事／作業／移動／運動／余暇／学習／その他）
  + 既存の休憩は `break` カテゴリで色分け
- カテゴリ別の合計時間をタイムライン下部に表示
- 変更ファイル：`src/records.html`、`src/records.css`、`src/records.js`

#### タイムライン記入モーダルを追加
- `?` セグメントをクリックすると入力モーダルを表示
- カテゴリ大ボタン1タップ + メモ（任意） + 時間調整（time入力 × 2）
- 6時間以上のギャップは「睡眠」を初期選択
- 保存時に `source: 'timeline'` フラグを立てて `mt_logs` に追記

#### タイムライン入力を作業時間集計から切り離す
- ヘルパー `isTimelineEntry()` / `countsAsWork()` を追加
- カレンダーのヒートマップ／月計、日別ヘッダーの作業時間、「作業を追加」のタスク候補からタイムライン入力分を除外
- タイムライン視覚化とセッション一覧、Excel出力には全記録を含めて表示
- 旧データ互換：`source: 'manual' + category` のレコードもタイムライン入力として扱う

#### 直近のビルド
- リリースビルドを2回実行（実装直後 + 集計分離修正後）、いずれも成功
  - `src-tauri/target/release/minimal-timer.exe`
  - インストーラ：`bundle/msi/*.msi`、`bundle/nsis/*-setup.exe`

### 次回やること
