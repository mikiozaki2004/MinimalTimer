# 開発日誌

---

## 2026-03-12 (2)

### やったこと

#### タイマー終了後に記録が表示されないバグを修正
- **原因**: `records.js` の `renderSummary()` 内で `getAggregated()` の返り値（オブジェクト配列 `{ name, sec, details }`）を配列として分割代入していた
  - `entries.reduce((s, [, v]) => ...)` → オブジェクトは iterable でないため `TypeError` が発生
  - `entries[0]?.[1]` → `undefined` になる
- **修正**: `records.js` の96〜97行目をオブジェクト分割代入に修正
  - `entries.reduce((s, [, v]) => s + v, 0)` → `entries.reduce((s, { sec }) => s + sec, 0)`
  - `entries[0]?.[1] ?? 1` → `entries[0]?.sec ?? 1`
- データ自体はタイマー終了時に正常保存されていたが、集計ビューのクラッシュにより表示されていなかった

### 次回やること

- 記録ウィンドウに月ごとの記録を表示できるようにする
- スマホ対応（同じ機能をモバイルで使えるようにする）
- ボタンのアニメーション改善

---

## 2026-03-12

### やったこと

#### タスク「詳細」機能の実装
- `main.js`: タスクのデータ構造を `string[]` → `{ name, details[] }[]` に変更。既存データの自動移行あり
- `main.js`: `currentDetail` 状態を追加。タスクパネルに詳細の追加・選択・削除UIを実装（`[ ▸ タスク名 ×]` レイアウト）
- `main.js`: ログに `detail` フィールドを追加。休憩モード時の退避・復元にも対応
- `records.js`: サマリービューでタスクごとの詳細内訳を表示。リストビューで `タスク / 詳細` 形式で表示
- `records.css`: 詳細内訳のスタイル追加（左ボーダー付きインデント）
- バグ修正: 詳細追加フィールドクリック時にパネルが閉じる問題（`stopPropagation` 追加）
- バグ修正: タスクパネル内スクロールが時間設定に影響する問題（`wheel` イベント伝播を停止）

#### UIアニメーション改善
- `style.css`: タスクパネル・ポモドーロパネルの開閉にフェードイン・アウトアニメーションを追加
- `style.css`: ホバー時のボタン表示に `scale` アニメーションを追加
- `style.css`: プライマリボタン（`.controls`）の中央揃えを修正（`translateX` と `scale` の競合を解消）
- `style.css`: 三角トグルボタンのサイズを拡大して視認性向上

### 次回やること

- 前回のものを引き継いで作業する

---

## 2026-03-11 (2)

### やったこと

#### 記録ウィンドウのデータが更新されないバグを修正
- 原因: 記録ウィンドウは初回表示時のみデータを読み込み、2回目以降は再読み込みしなかった
- `src-tauri/src/lib.rs`: 既存ウィンドウを表示する際に `win.eval("window.refreshRecords()")` を呼び出すよう変更
- `src/records.js`: `window.refreshRecords()` 関数を追加。localStorage から最新の記録を読み直してリレンダリングする

### 次回やること

- ボタンのアニメーション改善
- 記録ウィンドウに月ごとの記録を表示できるようにする
- スマホ対応（同じ機能をモバイルで使えるようにする）

---

## 2026-03-11

### やったこと

#### サブボタン配置を円の最下部アークに集中
- `style.css`: 6つのサブボタン（テーマ・モード・休憩・ポモドーロ・タスク・記録）を r=100px で40°〜140° に均等配置し、円の下端に集中するデザインに変更

#### .exe起動時にデータが消える問題の調査
- 原因判明: 開発時（`http://localhost:1420`）とリリース時（`http://tauri.localhost`）でlocalStorageのオリジンが異なり、データが別々に保存されていた
- `tauri-plugin-store` による一元化を試みたが、バンドラーなし構成では動的importが機能せず断念
- `src/store.js`: localStorage をラップする抽象化レイヤーとして残置（将来の移行に備える）
- `.exe` を使い続ける限りデータは正常に保存・引き継ぎされることを確認
- 過去の開発版データの移行はユーザー判断によりスキップ

### 次回やること

- なし

## 2026-03-10

### やったこと

#### ポモドーロ機能実装
- `main.js`: ポモドーロ状態管理（`pomo` オブジェクト）、セット数設定、作業→休憩の自動切替、ドット表示による進捗表示
- `index.html`: ポモドーロ設定パネル（セット数 ±、開始ボタン）、`.pomo-status` ドット表示領域を追加
- `style.css`: ポモドーロドット・パネルのスタイル、`#btn-pomo.active` / `#btn-break.active` 常時表示
- バグ修正: ポモドーロボタンクリック時に即座にパネルが閉じる問題（イベントバブリング）を `stopPropagation` で解消

#### ウィンドウサイズ変更機能
- `main.js`: Ctrl+スクロールで 140〜560px の範囲でウィンドウサイズ変更、`zoom` プロパティでスケーリング、localStorage に保存
- `lib.rs`: `resize_window` コマンド追加、`CURRENT_SIZE` 静的変数でサイズ保持、`dismiss_completion` に反映

#### ボタンレイアウト再設計
- `index.html`: プライマリコントロール（開始/一時停止/リセット）と、サブコントロール（モード切替・テーマ・休憩・ポモドーロ・タスク・記録）を分離
- `style.css`: サブコントロールを円の最下部アーク（r=100px, 40°〜140°）に弧状に配置、プライマリコントロールは円の中央下部に固定

#### リリースビルド修正
- `lib.rs`: `open_devtools` を `#[cfg(debug_assertions)]` で囲みリリースビルドエラーを解消

### 次回やること

- デスクトップにショートカットを表示させる

---

## 2026-03-07

### やったこと

#### 記録ウィンドウの不具合修正
- `lib.rs`: ウィンドウが破棄された後に再作成するフォールバック追加、`on_window_event` で close を hide に変換
- `records.html` / `records.css`: ✕ボタンを `data-tauri-drag-region` の外に移動し、クリックが正しく動作するように修正
- `records.js`: ✕ボタンの close を `invoke('hide_records')` 経由に変更し、Tauri JS APIの互換性問題を回避

#### 開発者ツール起動の改善
- `lib.rs`: 記録ウィンドウの devtools 自動オープンを削除、`open_records_devtools` コマンドを新設
- `index.html` / `style.css` / `main.js`: 記録ボタンの右クリックで「開発者ツールと共に開く」メニューを表示

#### カウントダウン終了通知
- `lib.rs`: `notify_completion` / `dismiss_completion` コマンドを追加（ウィンドウ拡大→画面中央移動→元に戻す）
- `style.css`: 2秒周期のフェードイン・アウト点滅アニメーション
- `main.js`: 終了時に通知開始、クリック/ボタン操作で解除するロジック

#### 休憩モード機能
- `index.html`: ☕休憩ボタンを追加
- `main.js`: 休憩モードの状態管理・切替ロジック（5分カウントダウン、ログに `isBreak` フラグ付与）
- `style.css`: 休憩中はリング色を緑系に変更
- `records.js` / `records.html` / `records.css`: 作業時間と休憩時間を分離表示、☕マーク付き

### 次回やること

- ポモドーロ機能の実装
- UI（主にボタン類）の改善

---

## 2026-03-04（一時停止ボタン実装）

### やったこと

#### 一時停止ボタンの追加
- `index.html`: ⏸ ボタン（`btn-pause`）を追加。実行中のみ表示
- `index.html`: 既存の停止ボタンのアイコンを ⏸ → ⏹（四角）に変更し、「記録して停止」であることを明示
- `main.js`: `btnPause` ハンドラ実装 — `st.running = false` のみ実行し `logSession()` を呼ばない。`st.sessionStart` を保持するため、▶ で再開すると元の開始時刻から継続してカウントされる
- `main.js`: `btnPlay` / Enter キーで `st.sessionStart` を上書きしないよう修正（`=== null` チェック追加）
- `main.js`: `btnReset` / `btnMode` で `st.sessionStart = null` を明示的にクリア

### 次回やること

（未記入）

---

## 2026-03-04（記録機能実装）

### やったこと

#### 記録機能の実装
- `main.js`: タイマー開始時に `st.sessionStart` を記録し、`logSession()` で `startedAt`・`endedAt`・`mode`・`id` を保存するよう変更
- `records.html`: 期間タブ（今週・今月）とビュー切替（集計・一覧）のサブヘッダーを追加
- `records.js`: 週・月フィルタ、セッション一覧ビュー（日付グループ＋時刻範囲表示）を実装
- `records.css`: サブヘッダー・一覧ビュー用スタイルを追加

#### 記録ウィンドウが表示されなかった原因と修正
**原因**: Rust コードで `WebviewWindowBuilder` を使って実行時に動的にウィンドウを生成すると、Tauri v2 + WebView2（Windows）環境でウィンドウの WebView が初期化に失敗し、HTML/CSS/JS が一切レンダリングされない現象が発生。ウィンドウ自体は作成されるが中身が完全に透明・空白になり、JS も動かないためボタンも反応しなかった。

**修正**: `tauri.conf.json` の `windows` 配列に records ウィンドウを `"visible": false` で事前宣言し、アプリ起動時から裏で読み込んでおく方式に変更。`open_records` コマンドは `show()` と `set_focus()` を呼ぶだけになった。閉じるボタンは `close()` から `hide()` に変更。

### 次回やること

（未記入）

---

## 2026-03-04

### やったこと
- デスクトップのショートカットアイコンが低解像度（16x16のみ）でぼやけていた問題を解消
- 元画像（`icon/名称_未_設定.png`、2048x2080px）を1024x1024にクロップ
- `tauri icon` コマンドで全サイズ・全形式のアイコンを自動生成（`src-tauri/icons/` 以下に配置）
- アプリをリビルドし、インストーラー・EXE を更新

### 次回やること
- 生成したアイコン（`src-tauri/icons/`）を Tauri の設定に正しく紐付け、デスクトップショートカットに反映させる
